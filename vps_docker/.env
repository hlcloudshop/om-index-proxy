# Broker API key (required)
INDEX_API_KEY=om_xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Public origin sent to the broker as the `Origin` header.
# Leave empty to auto-detect from incoming request headers (Host / X-Forwarded-Host).
WORKER_ORIGIN=
